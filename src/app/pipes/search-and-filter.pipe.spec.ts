import { SearchAndFilterPipe } from './search-and-filter.pipe';

describe('SearchAndFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchAndFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
